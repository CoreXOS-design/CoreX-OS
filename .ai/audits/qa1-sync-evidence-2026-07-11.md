# QA1 sync + sanitize — evidence pack
> Run: 20260711-180919  ·  host live=`nexus_os` → qa1=`corex_qa1`  ·  supervised (manual) run
> Cron is OFF. The nightly copy arms **Monday** on Johan's word (see scripts/qa1/README.md).

Preconditions: target=qa ✓ · live/qa1 DBs distinct ✓ · qa1 mail=Mailpit 127.0.0.1:1025 ✓ · qa1 WAHA empty ✓ · Mailpit up ✓
Live pending jobs at snapshot: **0** (these do NOT cross into qa1 — purged in Phase 5).
Live dump: `/var/tmp/qa1-sync/live-nexus_os-20260711-180919.sql.gz` (111M), mysqldump --single-transaction (live never locked, never written).
qa1 quiesced BEFORE load: maintenance ON + worker `corex-qa1-queue.service` stopped.
Loaded live snapshot into `corex_qa1` (qa1 tables overwritten; live untouched).
**Queue purge (live-intended jobs killed before the worker resumes):** jobs **0 → 0**, job_batches 0 → 0, failed_jobs 4697 → 0.
**WhatsApp neutered:** active devices 1 → 0 (session + token nulled).
**Syndication cursors cleared:** pp_event_feed_settings continuation keys remaining = 0 (qa1 never resumes a live PP feed cursor; PP already sandbox + P24 import disabled in qa1 .env).
**Storage strategy applied:** one-way `rsync -a --delete` live→qa1 (live = source, read-only; never a symlink into live). Number of regular files transferred: 20,923 Total transferred file size: 2,623,495,533 bytes 
- ✅ queued jobs purged (jobs table = 0)
- ✅ no active WhatsApp devices
- ✅ qa1 WAHA base url empty
- ✅ P24 import disabled in qa1 .env
- ✅ PP in sandbox in qa1 .env
- ✅ mailer delivers to Mailpit, not real SMTP (proof token found in Mailpit)

**Resume order honoured:** site + worker resumed ONLY after the gate passed (worker `corex-qa1-queue.service` start → `artisan up`).

## RESULT: ✅ SUPERVISED SYNC + SANITIZE PASSED

---
_Generated by scripts/qa1/sync-from-live.sh. Live was read-only throughout (mysqldump --single-transaction); staging / qatesting2 / live workers untouched._

## Independent post-run verification (not the script's own word)
- qa1 site: `GET https://qatesting1.corexos.co.za/` → **HTTP 302** (login redirect — UP, not 503).
- qa1 worker `corex-qa1-queue.service`: **active**. Maintenance file gone (**UP**).
- `corex_qa1` assertions: jobs=**0**, failed_jobs=**0**, active WA devices=**0**, PP cursors=**0**.
- Live data landed — qa1 vs live row counts: properties 5579/5579 · deals 139/139 · users 48/48 · documents 1598/1598 · contacts 8201/8202 (1-row live drift during the ~13-min run — expected: live kept moving; the snapshot is consistent to 18:09:19).
- Mailpit `/api/v1/search?query=QA1SYNCPROOF-20260711-180919` → `messages_count: 1` (the routing proof landed in Mailpit, not real SMTP).
- **Cron: OFF** — no qa1 sync entry in root crontab. Arms **Monday** on Johan's word.

## Note on the queue purge count
Live's *pending* `jobs` table happened to be **0** at the 18:09 snapshot (workers had just drained it), so jobs 0→0. The purge mechanism is still demonstrated: **4,697 `failed_jobs` rows** carried in from live were purged to 0, and the gate asserts `jobs=0` regardless of snapshot timing. On a Monday-night run with a fuller live queue the same TRUNCATE removes whatever pending live-intended sends are present before the worker resumes.
